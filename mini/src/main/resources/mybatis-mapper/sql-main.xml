<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
	
	<mapper namespace="com.example.mini.mapper.MainMapper">
		
		<!--진행중 펀딩 리스트-->		
		<select id="selectFundingList" parameterType="hashmap" resultType="com.example.mini.model.Main">
			<![CDATA[
				SELECT
					A.FUNDING_NO,
					FUNDING_NAME,
					FUNDING_SUMMARY,
					FUNDING_SCRIPT,
					FUNDING_START_DT,
					FUNDING_END_DT,
					FUNDING_CLOSE_YN,
					FUNDING_GOAL_CNT,
					FUNDING_PRICE,
					FORMAT(FUNDING_PRICE , 0) AS FUNDING_PRICE2,
					CDATETIME,
					UDATETIME,
					CNT,
					TIMESTAMPDIFF(DAY, NOW(), FUNDING_END_DT) AS D_DAY,
					IMG_PATH,
					DATE_FORMAT(FUNDING_END_DT,'%k') AS END_TIME
					
				FROM T4_FUNDING A
				LEFT JOIN (SELECT FUNDING_NO, count(FUNDING_NO) as CNT
							FROM T4_FUNDING_MEMBER
							GROUP BY FUNDING_NO) B ON A.FUNDING_NO = B.FUNDING_NO
				LEFT JOIN (SELECT FUNDING_NO, IMG_PATH
							FROM T4_F_IMAGE
							WHERE THUMBNAIL_YN = 'Y'
							GROUP BY FUNDING_NO) C ON A.FUNDING_NO = C.FUNDING_NO
				WHERE  FUNDING_START_DT < NOW()
				
				ORDER BY RAND() LIMIT 2]]>
				
		</select>
		<!--진행중 펀딩 리스트 끝-->
		
		
		
	</mapper>