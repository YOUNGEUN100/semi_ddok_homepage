<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
	<mapper namespace="com.example.mini.mapper.SampleMapper">
		<select id="selectlanList" parameterType="hashmap" resultType="com.example.test1.model.Bbs">
		SELECT
			BOARD_NO,
			USER_ID,
			TITLE,
			CONTENT,
			HITS,
			BOARD_KIND,
			FINISH_YN,
			CATEGORY,
			CDATETIME,
			UDATETIME,
			DELETE_YN,
			SECRET_YN,
			ANSWER_YN  
		FROM T4_BOARD
		WHERE ;
	</select>
	</mapper>